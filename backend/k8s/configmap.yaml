apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-configmap
  namespace: mern-app
data:
 MONGO_CONN_STR: mongodb://mongo-database-svc:27017/todo?replicaSet=rs0&authSource=admin&authMechanism=SCRAM-SHA-256
 
 #mongodb://$(MONGO_USERNAME):$(MONGO_PASSWORD)@mongo-database-svc:27017/todo?replicaSet=rs0
 
 
 #for operator setup without authentication
 #MONGO_CONN_STR: mongodb://mongo-database-svc:27017/todo?replicaSet=rs0
 
 

 #for statefulset setup without authentication
 #mongodb://database-0.db-service:27017,database-1.db-service:27017,database-2.db-service:27017/todo?replicaSet=rs0
 #mongodb://username:password@host1,host2,host3/DATABASE_NAME?options         ----> normal structure for MongoDB connection string with replica set